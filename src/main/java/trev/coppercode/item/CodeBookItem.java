package trev.coppercode.item;

import net.minecraft.world.item.Item;

/** Future container item for bundled instructions, scripts, and editor-driven content. */
public final class CodeBookItem extends Item {
	public CodeBookItem(Properties properties) {
		super(properties);
	}
}

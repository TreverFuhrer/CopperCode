package trev.coppercode.script.parser;

import java.util.List;
import trev.coppercode.script.lexer.Token;

/**
 * Placeholder parser.
 *
 * TODO Define the real Cogscript grammar, statement parsing, expression parsing, block handling, and error recovery.
 */
public final class CogscriptParser {
	public ParserResult parse(List<Token> tokens) {
		return ParserResult.unsupported("Cogscript parser scaffold is present, but grammar implementation is still TODO.");
	}
}

package trev.coppercode.script.validate;

import trev.coppercode.script.ast.RoutineNode;

/**
 * Placeholder semantic validator.
 *
 * TODO Validate command names, argument counts, identifier resolution, type compatibility, and future capability restrictions.
 */
public final class CogscriptValidator {
	public ValidationResult validate(RoutineNode routine) {
		return ValidationResult.unsupported();
	}
}

package trev.coppercode.registry;

/** Owns screen handler registrations for menus that also need client screens. */
public final class ModScreenHandlers {
	private ModScreenHandlers() {
	}

	public static void initialize() {
		registerScreenHandlers();
	}

	private static void registerScreenHandlers() {
		// TODO Register screen handlers here before pairing them with client screens.
	}
}
